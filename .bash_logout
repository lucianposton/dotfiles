# This file is sourced when a *login* shell terminates. SYS_BASH_LOGOUT, e.g.
# /etc/bash/bash_logout, will be sourced afterwards, only if bash was compiled
# with SYS_BASH_LOGOUT defined.

# Clear the screen for security's sake.
clear

